import React from 'react';
import { render, fireEvent, screen } from '@testing-library/react';

import MediaSearchFrom from "../index";

describe("MediaSearchFrom Component", () => {

    it.todo("should render the markup");
});